import { tab } from "openrct2-flexui";

export const debuggerTab = () => {
  return tab({
    image: {
      frameBase: 5367,
      frameCount: 8,
      frameDuration: 8,
    },
    content: [
      // todo add debugger content
    ],
  });
};
